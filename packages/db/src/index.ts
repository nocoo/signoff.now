/**
 * @signoff/db — D1 migrations live in ./migrations.
 * Application SQL types will land here as the Model layer grows.
 *
 * Must match the highest applied migration number under migrations/.
 */
export const DB_SCHEMA_VERSION = 5;
