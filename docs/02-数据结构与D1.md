# 02 — 数据结构与 D1 Schema

> 状态：已确认（含 0002 正确性修订）  
> 依赖：[01-项目定位](./01-项目定位.md)  
> 产出：中立实体模型 + Cloudflare D1 DDL；`0001` 建库 + `0002` 正确性/索引；可强制全量重算

---

## 1. 目标

1. Web CRUD：Developer / Team / Tag / Repo / Settings（**软删除**）  
2. 管线幂等写入 Activity / Score；配置变更可检测 **stale** 并 **全量重算**  
3. `external_ref` 使用 **稳定 provider GUID / revision / thread id**，不把可变名称/邮箱当永久幂等键  
4. provider 中立（`ado` | `github`）；一期只写 `ado`

| 路径 | 说明 |
|:-----|:-----|
| `packages/db/migrations/0001_initial.sql` | 初始表结构 + settings 基础种子 |
| `packages/db/migrations/0002_correctness_and_indexes.sql` | 软删、RESTRICT、config_version、索引、json_valid |
| `packages/db/migrations/0003_repo_guid_and_score_index.sql` | 全局 external_id 唯一、ADO GUID 强制、Score 复合索引 |
| `packages/db/src/migrate.ts` + `migrate.test.ts` | 内存库套用全量 migration（**bun:test + bun coverage**） |
| `wrangler.toml` | D1 binding、`migrations_dir` |

**禁止改写已应用的 `0001`**；一切修正走新 migration。

---

## 2. 设计原则

| 原则 | 决策 |
|:-----|:-----|
| 单实例 | 无 tenant_id |
| 主键 | TEXT UUID |
| 时间 | unix **秒** UTC；自然日 `day_key` + **config_version** |
| 软删除 | `archived_at`；活跃行 UNIQUE 用 partial index；**不**硬删有历史的 Developer/Repo |
| 历史 FK | Activity/Score → Developer/Repo 使用 **ON DELETE RESTRICT** |
| 幂等 | `activities.external_ref` 全局 UNIQUE；键基于 **稳定 id**（§5） |
| 配置失效 | `pipeline_config_version` + `scores_stale`；改 timezone/weights/**suffixes**/alias 必须 bump，并 **rematch raw → Activity → Score**（§3） |
| 聚合去重 | 「同人同 WI 同日一次」在 **Score 计算层**，不在 external_ref 用 day_key 压扁多人 |
| JSON | `settings.value`、`scores.breakdown_json`、`activities.meta_json` 使用 `json_valid()` |
| 读写边界 | Activity/Score 仅管线写 |

---

## 3. 配置版本与强制重算（P1）

### 3.1 Settings 键

| key | 类型 | 说明 |
|:----|:-----|:-----|
| `timezone` | string | 默认 `Asia/Shanghai` |
| `email_suffixes` | string[] | 默认 `["microsoft.com"]` |
| `activity_weights` | object | 见 01 §6.3 |
| `pipeline_config_version` | number | **单调递增**；默认 `1` |
| `scores_stale` | boolean | 配置变更后 `true`，全量重算成功后 `false` |
| `scores_stale_reason` | string \| null | UI 提示 |

`value` 一律 JSON 文本，且 `CHECK (json_valid(value))`。

### 3.2 写入语义（配置类变更）

| 操作 | 必须 |
|:-----|:-----|
| Web 修改 `timezone` 或 `activity_weights` | `pipeline_config_version += 1`；`scores_stale = true`；reason；触发 **全量重算**（见 §3.4，至少 day_key + Score） |
| Web 修改 `email_suffixes` | 同上 bump + stale；触发 **重新匹配 raw → 重建 Activity → 重算 Score**（§3.4 全链路，**不得**只刷 `unmatched_identities`） |
| Web 修改 Developer.`alias`（或归档/恢复影响匹配集） | 同上：必须 **rematch raw → rebuild Activity → recompute Score** |
| 管线日常增量 ingest | 用**当前** alias/后缀匹配；写入 `activities.config_version`；按当前时区算 `day_key`；更新相关 Score |
| 全量任务成功结束 | `scores_stale=false`；`scores_stale_reason=null` |

> **错误说法（已废止）：**「改后缀/alias 只影响未映射列表」。  
> 历史 raw 中昨日未命中的 uniqueName，在 alias/后缀变更后可能命中；Activity 集合与 Score **都会变**，必须从 raw 重放。

### 3.3 UI

- 若 `scores_stale === true`：热力图/对比展示 **stale 横幅**，禁止当作最终考核结果。  
- 提供「触发全量重算 / 重匹配」（本机 skill/script——鉴权见 06）。

### 3.4 全量重匹配与重算流水线

有序步骤（同一 `pipeline_config_version`）：

1. **读取**当前 settings（timezone、suffixes、weights）与全部 **enabled 且未归档** Developer / Repo。  
2. **扫描** `.data/raw`（或约定 raw 存档）中相关 org/project/repo/WI。  
3. **匹配**：对每条事件身份 `uniqueName` 跑 `alias@suffix` 规则；未命中 → `unmatched_identities`，**不写** Activity。  
4. **重建 Activity**：  
   - 推荐：删除（或标记废弃）旧 `config_version < 当前` 的 activities，再按 §5 `external_ref` UPSERT 新行；或整表按 ref 幂等覆盖。  
   - 每行写入当前 `config_version` 与新 `day_key`。  
5. **重算 Score**：按 `(developer_id, day_key)` 聚合当前版本 activities（含日折叠规则，见 03）；写入 `scores.config_version`。  
6. 清除 stale 标志。

仅改 timezone/weights、**匹配集不变**时：可跳过步骤 3 的「身份重绑」，但仍须按新时区重写 `day_key` 并按新权重重算 Score（仍建议走统一全量任务，避免分叉实现）。

### 3.5 行级字段

| 表 | 列 | 说明 |
|:---|:---|:-----|
| `activities` | `config_version` | 生成该 `day_key` / 匹配结果时的配置版本 |
| `scores` | `config_version` | 计算该分时的配置版本 |

查询「当前有效积分」：应用层要求 `scores_stale = false` 且 `config_version = pipeline_config_version`。

---

## 4. ER 概览

```
settings (KV + config version / stale)
developers (archived_at) ──┬── developer_teams ── teams (archived_at)
                           └── developer_tags  ── tags (archived_at)
repos (archived_at, external_id)
activities ── RESTRICT ── developers
           ── RESTRICT ── repos?
scores     ── RESTRICT ── developers
unmatched_identities
ingest_runs
```

---

## 5. `external_ref` 稳定约定（P1）

**禁止**把可变的 org 显示名 / project 名 / repo 名 / 邮箱作为永久幂等身份。  
**禁止** `wi.updated:{day_key}` 作为唯一键（同一天多人对同一 WI 会冲突）。

### 5.1 稳定标识来源（ADO）

| 实体 | 稳定 id |
|:-----|:--------|
| Repository | `repos.external_id` = ADO **repository GUID**（绑定时写入） |
| Project | ADO **project GUID**（可存 meta 或拼进 ref；名称列仅展示） |
| Pull Request | `pullRequestId`（在 repo 内稳定）+ repo GUID |
| Identity（vote 主体） | ADO **identity id** 或 **descriptor**（事件载荷中的 id，不是 email） |
| Work Item | `System.Id` + project GUID |
| WI 修订 | **revision id** / update id（updates API） |
| PR thread/comment | **thread id** + **comment id**（threads API） |

名称列（`repos.org/project/name`）仍保留供展示与 az CLI 调用参数；**幂等键只用 GUID**。

### 5.2 推荐 ref 模板

| type | external_ref |
|:-----|:-------------|
| `pr.created` | `ado:pr:{repoGuid}:{prId}:created` |
| `pr.merged` | `ado:pr:{repoGuid}:{prId}:merged` |
| `pr.closed` | `ado:pr:{repoGuid}:{prId}:closed` |
| `pr.vote` | `ado:pr:{repoGuid}:{prId}:vote:{identityId}:{threadId}:{commentId}` |
| `pr.active` | `ado:pr:{repoGuid}:{prId}:iter:{iterationId}` |
| `wi.created` | `ado:wi:{projectGuid}:{wiId}:created` |
| `wi.closed` | `ado:wi:{projectGuid}:{wiId}:closed` |
| `wi.updated` | `ado:wi:{projectGuid}:{wiId}:rev:{revisionId}` |

### 5.3 Score 层去重（非 ref 层）

- **同人同 WI 同日多次 `wi.updated`**：保留多条 Activity（每 revision 一条），Score 聚合时对该 type **按日计一次权重**（或取 max/sum 策略在 03 定，默认「按日计一次」）。  
- **同人同 PR 同日多个 active iteration**：Score 层可按日折叠 `pr.active`。  
- 终态 PR（merged/closed）与 created 的优先级仍在 03 算法中处理。

---

## 6. 表定义（现行 = 0001 + 0002）

### 6.1 `settings`

| 列 | 约束 |
|:---|:-----|
| `key` | PK |
| `value` | NOT NULL，`CHECK (json_valid(value))` |
| `updated_at` | unix 秒 |

键见 §3.1。

### 6.2 `developers` / `teams` / `tags`

| 列 | 说明 |
|:---|:-----|
| `id` | UUID |
| `name` | 显示名 / 名称 |
| `alias` | 仅 developers；NOCASE |
| `color` | 仅 tags |
| `archived_at` | NULL=活跃；非 NULL=软删 |
| timestamps | |

**唯一索引（partial）：**

- `developers(alias) WHERE archived_at IS NULL`  
- `teams(name) WHERE archived_at IS NULL`  
- `tags(name) WHERE archived_at IS NULL`  

Web **默认不提供硬 DELETE**；归档设 `archived_at`。若强行 `DELETE` 且仍有 Activity → **RESTRICT 失败**。

### 6.3 关联表 + 反向索引

- `developer_teams(developer_id, team_id)`  
- `developer_tags(developer_id, tag_id)`  
- **索引：** `(team_id, developer_id)`、`(tag_id, developer_id)` — 避免按 Team/Tag 筛人时全表扫

### 6.4 `repos`

| 列 | 说明 |
|:---|:-----|
| `provider` | `ado` \| `github` |
| `org` / `project` / `name` | 展示与 az 参数（可变） |
| `external_id` | **稳定** repository GUID（ADO 必填推荐） |
| `enabled` | 是否参与采集 |
| `archived_at` | 软删 |
| `remote_url` | 可选 |

**索引 / 约束：**

- UNIQUE `(provider, org, project, name) WHERE archived_at IS NULL`（活跃名称坐标）  
- UNIQUE `(provider, external_id) WHERE external_id IS NOT NULL`（**含已归档**；GUID 永久占用）  
- CHECK：ADO 且 `enabled=1` 且未归档 ⇒ `external_id` 非空  

**重新启用仓库：** `UPDATE … SET archived_at = NULL`，**禁止**再 INSERT 同一 `(provider, external_id)`。

### 6.5 `activities`

| 列 | 说明 |
|:---|:-----|
| `developer_id` | FK **RESTRICT** |
| `type` | 开放字符串；一期约定见 01 |
| `occurred_at` | 事件 UTC 秒 |
| `day_key` | 当时 config 下的自然日 |
| `config_version` | 对应 `pipeline_config_version` |
| `provider` / `org` / `project` | 作用域（展示/过滤；幂等不依赖名称） |
| `repo_id` | PR 类有值；WI **NULL**；FK **RESTRICT** |
| `external_ref` | UNIQUE，§5 |
| `matched_unique_name` | 审计用邮箱串 |
| `meta_json` | 可选，`json_valid` |
| `ingested_at` | |

**索引：**

- UNIQUE `external_ref`  
- `(developer_id, day_key)`  
- `(developer_id, day_key DESC, occurred_at DESC)` — 明细时间线  
- `day_key`、`type`、`(org, project)`、`config_version`

### 6.6 `scores`

| 列 | 说明 |
|:---|:-----|
| `(developer_id, day_key)` | PK；developer FK **RESTRICT** |
| `config_version` | |
| `total` | 整型分 |
| `breakdown_json` | `json_valid` |
| `activity_count` | |
| `computed_at` | |

**索引：**

- `day_key`  
- `config_version`  
- **`(config_version, day_key, developer_id)`** — 跨开发者、按版本+日期范围的热力/排行（避免只走 config_version 后全表排序）

### 6.7 `unmatched_identities` / `ingest_runs`

同 0001；辅助未映射身份与运行审计。

---

## 7. 身份匹配（应用层）

与 01 一致，不因 §5 改变：

```
for suffix in email_suffixes:
  if lower(uniqueName) == lower(alias + "@" + suffix) → developer
```

- vote 的 `external_ref` 使用 **identity id/descriptor**；匹配 Developer 仍可用 uniqueName→alias。  
- bot / group：不建 Developer，不写 Activity。

---

## 8. 落盘与 WI 作用域

```text
.data/raw/ado/{org}/{project}/
  repos/{repo}/…          # PR 族
  workitems/…             # Project 作用域，多 repo 共享一份
  workitem-updates/…
```

`repos.external_id` / project GUID 应在 raw 或绑定表中可解析，供 §5 ref 使用。

---

## 9. 建库与迁库

```bash
bun run db:migrate:local
bun run db:migrate:remote
bun run db:list:remote
```

DDL 源：`packages/db/migrations/*.sql`（按文件名排序）。

测试（**Bun 原生 coverage**，不用 vitest v8）：

```bash
bun run --cwd packages/db test
bun run --cwd packages/db test:coverage   # bun test --coverage，阈值见 packages/db/bunfig.toml
```

内存库顺序应用 **0001+0002+0003**，断言表/索引/FK/json/全局 GUID 唯一/默认 settings。

`DB_SCHEMA_VERSION`（`packages/db/src/index.ts`）= 当前最高 migration 序号（现为 **3**）。

---

## 10. 验收清单

- [x] 0001 / 0002 远端已应用  
- [x] 0003：全局 external_id、ADO GUID 强制、Score `(config_version, day_key, developer_id)`  
- [x] external_ref 文档为 GUID/revision/thread  
- [x] 配置 / alias / 后缀变更 → **rematch raw → Activity → Score**  
- [x] migration 自动化测试 + Bun coverage 门禁  
- [x] 远端 0003 apply（执行环境确认）  

---

## 11. 后续

| 文档 | 内容 |
|:-----|:-----|
| 03 | [Web 模块模板](./03-Web模块模板.md)（前端/Worker 工程约定） |
| 04 | [Settings 设计](./04-Settings设计.md)（Web CRUD、CLI 读 D1、stale/rematch） |
| 03b | 重算算法、Score 日折叠（后续） |
| 04 | CLI 拉取 GUID/revision/threads 与 ref 生成 |
| 05 | Web 软删 CRUD + stale 横幅 |
| 06 | ingest / recompute 鉴权 |

---

**文档结束（02）**
